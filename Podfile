# Uncomment this line to define a global platform for your project
platform :ios, "12.0"

target "Label My Stuff" do
    
    #pod 'ZXingObjC', '~> 3.0.1' # Got an error in XCode 6, "Use of undeclared identifier '_description';
    pod 'ZXingObjC' #, '~> 3.1.0'
    pod 'ZBarSDK' #, '~> 1.3.1'
    #pod 'ECSlidingViewController', '~> 1.3.3' #Latest version that supports iOS 6 is v1.3 (v1.3.3), Had to modify code.
    pod 'BCGenieEffect' #, '~> 1.1' #Latest version
    #Apple's iAd network no longer live so using AdMod.
    #For iOS 6 support using Googe Mobile Ads SDK without Firebase as recommended (https://cocoapods.org/pods/Google-Mobile-Ads-SDK).
    pod 'Google-Mobile-Ads-SDK' #,'~> 7.10'

end

=begin
target "Label My Stuff Tests" do

end
=end
